spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:postgresql://ocalhost:5432/mystore
    username: postgres
    password: Viking07
  flyway:
    baseline-on-migrate: true
